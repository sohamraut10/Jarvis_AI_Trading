# Termux-compatible requirements (no torch/SB3 — RL retraining disabled,
# all paper-trading features work normally)

# ── Core API & async ──────────────────────────────────────────────────────────
fastapi==0.111.0
fastapi-cli==0.0.5
uvicorn==0.29.0          # no [standard] extra — avoids uvloop/httptools C deps
websockets==12.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Dhan broker SDK ───────────────────────────────────────────────────────────
dhanhq==2.0.1

# ── Numerical / ML (numpy installed via pkg, rest from pip) ───────────────────
pandas==2.2.2
scipy==1.13.0
scikit-learn==1.4.2
hmmlearn==0.3.2

# ── Async / scheduling ────────────────────────────────────────────────────────
apscheduler==3.10.4
aiofiles==23.2.1

# ── Persistence & logging ─────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0
structlog==24.1.0

# ── AI layer (optuna only — no torch/gym/SB3 on ARM) ─────────────────────────
optuna==3.6.1
