# ── Core API & async ─────────────────────────────────────────────────────────
fastapi==0.111.0
fastapi-cli==0.0.5      # pin cli shim to prevent fastapi upgrade cascade
uvicorn[standard]==0.29.0
websockets==12.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Dhan broker SDK ───────────────────────────────────────────────────────────
dhanhq==2.0.1

# ── Numerical / ML ────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
scikit-learn==1.4.2
hmmlearn==0.3.2          # HMM regime classifier

# ── Async / scheduling ────────────────────────────────────────────────────────
apscheduler==3.10.4
aiofiles==23.2.1

# ── Persistence & logging ─────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0
structlog==24.1.0

# ── LLM inference (Layer 5 AI Brain) ─────────────────────────────────────────
anthropic>=0.28.0
openai>=1.30.0

# ── AI self-improvement layer ─────────────────────────────────────────────────
stable-baselines3==2.3.0
gymnasium==0.29.1
optuna==3.6.1
torch==2.2.2          # SB3 backend (CPU-only wheel is fine)

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
black==24.4.2
ruff==0.4.4
